import * as assert from "assert";
import { getRandomNumber } from "./aiStrategy";

describe('', () => {
    it('get random number ', function () {
        console.log(getRandomNumber(10))
    });
});
